# ============================================
# SECURITY - JWT Configuration
# ============================================
# REQUIRED in production: Must be at least 32 characters
# Generate secure secrets: openssl rand -hex 32
JWT_ACCESS_SECRET=your-super-secret-access-key-change-this-in-production-min-32-chars
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-this-in-production-min-32-chars

# Optional RS256 keys (PEM format). If using RS256, set these and leave HS secrets as fallback
# JWT_PRIVATE_KEY=""
# JWT_PUBLIC_KEY=""

# ============================================
# DATABASE Configuration
# ============================================
DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# ============================================
# REDIS Configuration
# ============================================
REDIS_URL=redis://localhost:6379

# ============================================
# EMAIL Configuration (nodemailer)
# ============================================
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=example_user
SMTP_PASS=example_password
SMTP_FROM=noreply@smartrestaurant.com

# ============================================
# CORS Configuration
# ============================================
FRONTEND_URL=http://localhost:5173
PRODUCTION_FRONTEND_URL=https://your-app.vercel.app

# ============================================
# GOOGLE CLOUD STORAGE Configuration
# ============================================
GCS_PROJECT_ID=your-gcp-project-id
GCS_BUCKET_NAME=smart-restaurant-images
GCS_KEY_FILE=path/to/service-account-key.json

# ============================================
# STRIPE PAYMENT Configuration
# ============================================
# Get your keys from: https://dashboard.stripe.com/test/apikeys
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here

# ============================================
# PRODUCTION DEPLOYMENT NOTES
# ============================================
# 1. Set NODE_ENV=production
# 2. Generate strong JWT secrets (min 32 chars): openssl rand -hex 32
# 3. Configure production database URL
# 4. Set production frontend URL for CORS
# 5. Configure production SMTP credentials
# 6. Ensure Redis URL points to production instance
#
# Example production values:
# NODE_ENV=production
# JWT_ACCESS_SECRET=a1b2c3d4e5f6...32chars
# JWT_REFRESH_SECRET=z9y8x7w6v5u4...32chars
# DATABASE_URL=postgresql://prod_user:secure_pass@prod-db.example.com:5432/prod_db
# REDIS_URL=redis://prod-redis.example.com:6379
# PRODUCTION_FRONTEND_URL=https://your-frontend.vercel.app
# LOG_LEVEL=warn


# Google Cloud Storage
GCS_PROJECT_ID=project-name
GCS_BUCKET_NAME=bucket-name
GCS_KEY_FILE=./service-account.json
GCS_CLIENT_EMAIL=service_account_name@project-name.iam.gserviceaccount.com